namespace WDBtoPKT
{
    public static class Program
    {
        public static void Main(string[] args)
        {
            // C++/CLI can only be compiled to a dll, so we have this dummy runner
            WDBtoPKT.Run(args);
        }
    }
}
